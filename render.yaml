services:
  - type: web
    name: cspam-backend
    env: php
    plan: standard
    buildCommand: |
      composer install --prefer-dist --no-dev --no-interaction --optimize-autoloader
    startCommand: bash docker/render-start.sh
    envVars:
      - key: PHP_VERSION
        value: "8.2"
